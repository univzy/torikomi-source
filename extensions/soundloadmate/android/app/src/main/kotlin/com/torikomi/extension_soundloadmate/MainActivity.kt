package com.torikomi.extension_soundloadmate

import android.app.Activity

/**
 * Discovery anchor for the main Torikomi app's <queries> filter.
 * Never launched; just exposes an intent-filter so the extension is visible.
 */
class MainActivity : Activity()
